name: Inferno Container
on:
  push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: build container
        run: docker build -t dievri/inferno-os:latest .
